# Shared Contracts

This project contains shared models and protocol definitions used by the Alfa CLI, server, and desktop application.

## Key models

- `DeviceMetadata` — device-specific identification and pairing state
- `PolicyRule` — shared policy contract for enforcement and sync

## Usage

Add a project reference from `cli`, `server`, and `app/desktop` to `shared/Alfa.Shared.csproj`.
