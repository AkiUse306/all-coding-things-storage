# Alfa Web Dashboard

The web module is a Blazor Server dashboard for remote device control, policy monitoring, and telemetry insights.

## Run locally

```bash
cd web
dotnet run
```

## Build

```bash
dotnet build -c Release
```
